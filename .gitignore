# Environment / secrets
.env
*.env.local

# Local ad-hoc MCP connector config (embeds a real bearer token) -- used to drive mcp-server
# interactively from a Claude Code session during manual testing, not part of the deliverable.
.mcp.json

# Python
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
.venv/
venv/
dist/
build/

# Docker volumes mounted locally (if any)
pgdata/
redisdata/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Chatwoot storage bind mount (local dev only)
chatwoot_storage/
